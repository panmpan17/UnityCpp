# Unity C++ Library


## Compile c++ library

### Enviroment Setup (for mac)

**gcc**
`brew install gcc `

**gmingw-w64**
Compile c++ to .dll for windows platform
`brew install mingw-w64`

### Compile

```bash
cd demo-lib
make all # or make so, or make dll
```
